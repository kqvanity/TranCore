export class Word {
    title: string
    constructor(title: string) {
        this.title = title
    }
}

export class Pronunciation {
  title: string;
  url: string;
  constructor(title: string, url: string) {
    this.title = title;
    this.url = url;
  }
}

